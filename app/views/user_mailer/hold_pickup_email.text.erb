    Hi <%= @user.first_name %>,
    Your hold for the book <%= Book.find(@hold.book_id).title %> is ready for pickup.
    Your book will be available for pickup until <%= @hold.pickup_expiry.strftime("%A, %b %e, %Y") %> before it is released to the next hold in queue.